 # Database connection configuration
	# MySQL JDBC connection URL (example)
	db.url=jdbc:mysql://localhost/NAMEDB
	# Database username for authentication (example)
	db.username=USERNAME
	# Database password for authentication (example)
	db.password=PASSWORD


# Server configuration
	# Port number for the server (example)
	server.port=10
	# Port number for the web interface (example)
	web.port=11